<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/buttonGoldPressed"/> <!-- darker gold -->
            <corners android:radius="16dp"/>
            <stroke android:width="2dp" android:color="@color/buttonGold"/>
        </shape>
    </item>

    <!-- Selected state -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/buttonGold"/> <!-- gold background -->
            <corners android:radius="16dp"/>
            <stroke android:width="2dp" android:color="@color/buttonGoldPressed"/>
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white"/> <!-- white background -->
            <corners android:radius="16dp"/>
            <stroke android:width="2dp" android:color="@color/buttonGold"/> <!-- gold border -->
        </shape>
    </item>

</selector>
