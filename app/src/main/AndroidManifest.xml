<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--  Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="UnusedAttribute" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifePulse">

        <!-- Fragments as activities (for preview use only) -->
        <activity
            android:name=".ui.main.fragments.StepsFragment"
            android:exported="false" />
        <activity
            android:name=".ui.workers.HydrationEntry"
            android:exported="false" />
        <activity
            android:name=".ui.main.fragments.HydrateFragment"
            android:exported="false" />
        <activity
            android:name=".ui.main.fragments.RelaxFragment"
            android:exported="false" />
        <activity
            android:name=".ui.habits.CustomHabitActivity"
            android:exported="false" />
        <activity
            android:name=".ui.habits.AddHabitActivity"
            android:exported="false" />

        <!--  Home screen widget -->
        <receiver
            android:name=".ui.widget.HabitWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!--  Widget actions -->
        <receiver
            android:name=".ui.widget.WidgetActionReceiver"
            android:exported="true" />

        <!--  Full-screen alarm -->
        <activity
            android:name=".ui.alarm.AlarmActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />

        <!--  Habit & Hydration Reminder Receiver -->
        <receiver
            android:name=".notification.ReminderReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.example.lifepulse.ACTION_HYDRATION_REMINDER" />
            </intent-filter>
        </receiver>

        <!--  File Provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--  Splash Screen -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  Auth Screens -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.SignupActivity"
            android:exported="false" />

        <!--  Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!--  Worker Related -->
        <activity
            android:name=".ui.workers.HydrationWorkerActivity"
            android:exported="false" />
        <activity
            android:name=".ui.workers.HydrationHistoryActivity"
            android:exported="false" />
    </application>

</manifest>
